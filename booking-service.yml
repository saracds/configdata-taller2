spring:
  application:
    name: booking-service
  profiles:
    active: dev
server:
  servlet:
    context-path: /taller2/api/v1
  port: 9080
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka/


